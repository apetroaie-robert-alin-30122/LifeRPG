query GetCompletedQuests($userId: Int!) {
    getCompletedQuests(userId: $userId) {
        id
        title
        description
        xpReward
        category
        difficulty
        questType
        target
    }
}