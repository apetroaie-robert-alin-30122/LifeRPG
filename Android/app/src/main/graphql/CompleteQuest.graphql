mutation CompleteQuest($userId: Int!, $xpReward: Int!) {
    completeQuest(userId: $userId, xpReward: $xpReward) {
        username
        level
        experience
        xpGained
        leveledUp
        xpForNextLevel
    }
}